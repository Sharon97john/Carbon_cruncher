import styled from "styled-components";
import { Container } from "../../main";

const CardWrapper = styled(Container)`
  margin-top: -60px;
  padding: 0px 20px;
  display: flex;
  gap: 15px;
  overflow-y: hidden;
  overflow-x: auto;
  padding-bottom: 15px;
`;

export { CardWrapper };
